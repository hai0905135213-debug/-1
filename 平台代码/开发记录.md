# 开发记录

> 这份文档用于记录校园饭搭子平台的每一步开发过程、产品决策、技术决策、已完成事项和后续待办，避免切换开发环境或中断后丢失上下文。

## 记录规则

- 每次开始新任务前，先看本文件。
- 每次完成一个明确步骤后，把结果写入本文件。
- 重要产品决策、技术选型、数据库设计、接口设计、目录结构变化都要记录。
- 遇到问题、临时取舍、暂缓功能，也要记录原因。
- 不依赖聊天记录作为唯一上下文。

## 2026-07-07

### 已读取的项目资料

- 读取了 `校园饭搭子平台_完整申报书 人工润色版.docx`。
- 项目定位：面向高校学生的校园饭搭子智能匹配平台。
- 核心价值：不是单纯交友软件，而是“为了吃好这顿饭，顺手找个合适的人一起吃”的拼餐撮合工具。

### 第一阶段产品方向

第一阶段先做 MVP，不做大而全的平台。

MVP 核心闭环：

1. 用户登录并完善基础资料
2. 用户发起一个饭局
3. 其他用户浏览并加入饭局
4. 人数满足后成局
5. 线下就餐后进行简单评价

第一阶段暂缓：

- 复杂课表联动
- 商家抽佣
- PRO 会员
- 大规模推荐算法
- 多学校扩展
- 完整聊天系统

### 初步页面规划

- 首页：展示当前可加入饭局、推荐饭局、快捷发起入口
- 发起饭局页：填写吃什么、时间、地点、人数、预算、聊天模式
- 饭局详情页：查看发起人信息、参与人数、加入/退出饭局
- 我的饭局页：查看我发起的、我加入的、待确认的饭局
- 我的资料页：学生认证、口味偏好、信用分、历史评价

### 初步技术架构

- 前端：微信小程序或 uni-app
- 后端：Node.js + Express
- 数据库：MySQL
- 部署：腾讯云/阿里云轻量服务器

### 初步数据表

- `users`：用户表
- `profiles`：用户资料表
- `meal_posts`：饭局表
- `meal_participants`：饭局参与者表
- `reviews`：评价表
- `reports`：举报/爽约记录表
- `restaurants`：店铺/食堂窗口表

### 已完成文件

- 创建项目目录：`平台代码/`
- 创建项目说明：`平台代码/README.md`

## 2026-07-08

### 开发协作约定

- 用户要求：之后每做一步都记录下来，方便切换开发环境或后续继续开发。
- 已创建本文件：`平台代码/开发记录.md`
- 后续所有关键步骤都应同步记录到本文件。

## 2026-07-09

### 后端 MVP 骨架

- 用户说明：前端代码已有一部分但暂未上传，当前优先处理后端。
- 已创建 `backend/` 后端目录。
- 技术取舍：当前先使用 Node.js 原生 HTTP 服务，不引入外部依赖，避免安装依赖和网络环境影响联调。
- 当前数据暂存在内存 Map 中，字段按后续 MySQL 表结构预留。
- 已支持跨域请求，方便前端本地联调。
- 后端默认监听 `127.0.0.1:3001`，避免本机联调时暴露到局域网。

### 已完成接口

- `GET /api/health`：健康检查
- `POST /api/auth/login`：登录/注册，返回 MVP token
- `GET /api/profile`：获取当前用户资料
- `PUT /api/profile`：更新用户资料
- `GET /api/meals`：饭局列表，支持状态、校区、关键词、可加入筛选
- `POST /api/meals`：发布饭局
- `GET /api/meals/:mealId`：饭局详情
- `POST /api/meals/:mealId/join`：加入饭局
- `POST /api/meals/:mealId/leave`：退出饭局
- `GET /api/meals/mine`：我的饭局
- `POST /api/meals/:mealId/reviews`：发布评价

### 已完成文档

- `docs/API.md`：前端对接接口文档
- `docs/功能代码对照.md`：功能与代码位置对照表

### 后续建议

- 接入真实数据库 MySQL，把内存 Map 替换为数据库读写。
- 增加饭局取消、完成饭局、举报/爽约记录接口。
- 前端上传后，对照 `docs/API.md` 调整请求地址和字段。

### 数据库升级：内存 Map → SQLite

**决策背景：** 原来的内存 Map 每次重启数据全部丢失，联调时需反复造数据。正式 MySQL 对 MVP 阶段过重（需要独立部署、配权限、管连接池）。

**方案：** SQLite 作为过渡层。零配置、持久化、SQL 语法和 MySQL 一致，后续切 MySQL 只需换驱动和连接串。

**具体变更：**

- 安装依赖：`better-sqlite3`（同步 API，与现有代码风格一致）
- 新增 `backend/src/db.js`：数据库层，负责建表、CRUD、种子数据
  - 5 张表：`users`、`profiles`、`meals`、`meal_participants`、`reviews`
  - 所有返回数据已规范化为 camelCase，前端接口字段无变化
  - 首次启动自动建表 + 写入种子数据，后续重启数据持久保留
  - WAL 模式开启，读性能更好
  - 数据库文件位置：`backend/data/fanfan.db`
- 重构 `backend/src/server.js`：
  - 移除所有 Map 变量和手动 ID 计数器（`nextUserId` 等）
  - 改用 `db.js` 导出的函数进行数据读写
  - 接口逻辑、路由分发、错误处理保持不变
  - health 接口版本升级为 `0.2.0`，新增 `storage: "sqlite"` 字段

**测试结果（全部通过）：**
- ✅ 健康检查（含 storage 字段验证）
- ✅ 登录/注册（新用户创建、老用户复用）
- ✅ 饭局列表（筛选、搜索、仅可加入）
- ✅ 饭局详情（参与者列表、评价列表）
- ✅ 发布饭局（必填校验、自动加入创建者）
- ✅ 加入饭局（状态校验、人数上限、重复加入拦截）
- ✅ 退出饭局（发起人不能退出、正常退出）
- ✅ 饭后评价（权限校验、评分范围校验）
- ✅ 我的饭局（我创建的 + 我加入的分组）
- ✅ 重启持久化（新用户、新饭局、评价重启后均在）

### 新增：美食餐厅数据接口

**变更：**

- `backend/src/db.js`：
  - 新增 `restaurants` 表（name, food_type, campus, location, avg_price, rating, tags, description, status）
  - 新增 `normalizeRestaurant()` 规范化函数
  - 新增 `getRestaurant()`、`listRestaurants()`、`createRestaurant()`、`updateRestaurant()` CRUD 函数
  - 种子数据增加 6 家示例餐厅（二食堂麻辣香锅、东门米线店、三食堂轻食窗口、一食堂火锅、教工食堂牛肉面、南门小吃街烧烤）
- `backend/src/server.js`：
  - 新增 3 个路由：`GET /api/restaurants`（列表+筛选）、`GET /api/restaurants/:id`（详情）、`POST /api/restaurants`（新增，需登录）
- `docs/API.md`：新增接口 12-14 餐厅接口文档
- `docs/功能代码对照.md`：新增餐厅相关行

**设计说明：**
- meals 表的 `place` 字段暂不改动，保持自由文本。前端可以从餐厅列表选择填入 place，也可手动输入。
- `avgPrice` 单位为分（2200 = 22元），前端展示时除以 100。
- `tags` 以 JSON 数组存储，方便后续按标签推荐。
- 列表默认按评分降序排列。

**测试结果（8/8 通过）：**
- ✅ 餐厅列表（全量、foodType 筛选、keyword 搜索）
- ✅ 餐厅详情（含 tags 数组解析）
- ✅ 新增餐厅（含 tags 数组、必填校验）
- ✅ 未登录拦截、名称必填校验、404 处理
- ✅ 重启持久化（新增餐厅不丢）

### 项目当前文件结构

```
平台代码/
├── README.md
├── 开发记录.md
├── docs/
│   ├── API.md
│   ├── 功能代码对照.md
│   └── learn/                  # 🆕 学习文档（Obsidian 格式）
│       ├── 01-架构总览.md       # 技术选型、请求生命周期、目录结构
│       ├── 02-数据库层详解.md   # SQLite建表、normalize、CRUD模式、加表指南
│       ├── 03-server.js详解.md  # 路由分发、14个handler、错误码大全
│       ├── 04-项目约定与模式.md # 命名规则、字段对照表、代码模式
│       └── 05-API接口速查卡片.md # 14个接口一句话+curl示例
└── backend/
    ├── package.json
    ├── data/
    │   └── fanfan.db
    └── src/
        ├── server.js
        └── db.js
```

### 新增：学习文档（Obsidian 格式）

**目的：** 方便自己和其他 AI（如 Codex）快速理解项目，后续可整理进知识库。

**5 篇文档之间的关联：**
- [[01-架构总览]] → 全局视角，先读这篇
- [[02-数据库层详解]] → SQLite 怎么用、怎么加表
- [[03-server.js详解]] → 路由、handler 模板、怎么加接口
- [[04-项目约定与模式]] → 命名对照表、规范参考
- [[05-API接口速查卡片]] → curl 命令速查

每篇末尾有 ← 上一篇 / 下一篇 → 导航链，支持 Obsidian 双向链接。

### Git 同步与 SSH 推送完成

**背景：** 之前本机没有系统 Git，且 HTTPS 推送在 GitHub 认证/网络链路上不稳定。

**处理结果：**

- 已下载并使用便携版 Git：
  - 位置：`D:/平台/tools/PortableGit/`
  - 版本：`git version 2.55.0.windows.2`
- 已生成 GitHub 专用 SSH key：
  - 私钥：`C:/Users/blackey2007/.ssh/id_ed25519_github`
  - 公钥已添加到 GitHub 账号。
- 已验证 SSH 认证成功。
- 已将仓库远端地址切换为 SSH：
  - `git@github.com:hai0905135213-debug/-1.git`
- 已配置当前仓库固定使用该 SSH key：
  - `core.sshCommand=ssh -i C:/Users/blackey2007/.ssh/id_ed25519_github -o IdentitiesOnly=yes`
- 已成功推送本地提交：
  - `7f14214 Add frontend app`
- 已执行 `git fetch origin` 并确认本地 `main` 与 `origin/main` 一致。

**后续约定：**

- 之后在 `D:/平台/-1-main` 内可直接使用 Git 同步。
- 拉取更新：`git pull`
- 推送更新：`git push`
- 继续开发前先看 `git status`，避免未提交改动被混淆。

### 本地联调启动

**目标：** 在本机启动 localhost，先跑通后端 API 和前端静态预览。

**后端：**

- 工作目录：`D:/平台/-1-main/平台代码/backend`
- 使用便携版 Node：`D:/平台/-1-main/平台代码/tools/node-v24.18.0-win-x64/node.exe`
- 已安装后端依赖：`npm ci --registry=https://registry.npmmirror.com`
- 已通过语法检查：`npm run check`
- 已验证 `better-sqlite3` 可正常加载。
- 已启动后端服务：
  - 地址：`http://127.0.0.1:3001`
  - 健康检查：`GET /api/health`
  - 已确认返回 `ok: true`、`storage: "sqlite"`
- 已验证餐厅列表接口：
  - `GET /api/restaurants`
  - 已返回 6 条种子餐厅数据。

**前端预览：**

- 工作目录：`D:/平台/-1-main/平台代码/frontend`
- 已启动零依赖静态预览服务：
  - 地址：`http://127.0.0.1:5173`
  - 已确认 HTTP 状态码为 `200`

**当前说明：**

- 当前联调使用主仓库目录：`D:/平台/-1-main`
- `D:/平台/-1-git-sync` 是之前临时克隆目录，不作为后续开发主目录。
- 本次记录暂不提交、不推送，等后续开发阶段完成后统一同步。

### 前端按钮交互完善

**目标：** 当前 UI 风格已确认，继续把预览页和小程序页里“能看到但还不能点”的按钮补成可交互状态。

**静态预览页：**

- 修改 `frontend/preview-static/main.js`
  - 筛选胶囊支持点击切换选中态。
  - 首页“想去”按钮支持收藏/取消收藏。
  - 首页饭局卡片可进入详情。
  - 详情页返回按钮可回到上一页。
  - 详情页分享按钮显示提示。
  - 详情页“评价”显示待接入提示。
  - 详情页“想去/加入”点击后改为“已加入”。
  - 发布页表单支持创建一条临时饭局，并回到首页展示。
  - 我的页“新建饭局”可跳转发布页。
  - 编辑资料、口味标签、发布评价等按钮显示轻提示。
- 修改 `frontend/preview-static/style.css`
  - 新增 toast 提示样式。
  - 新增按钮禁用态。

**小程序/uni-app 页面：**

- 修改 `frontend/pages/home/index.vue`
  - 首页筛选项支持选中态切换。
  - 饭局卡片“想去”按钮支持收藏/取消收藏并 toast。
- 修改 `frontend/pages/find/index.vue`
  - 找人页榜单 Tab 支持切换。
  - 品类胶囊支持切换。
  - 清理多余的废弃模板，保留单一干净结构。
- 修改 `frontend/pages/feed/index.vue`
  - 关注/推荐 Tab 支持切换。
  - 场景胶囊支持切换。
  - 动态卡片“想去”按钮支持收藏/取消收藏。
- 修改 `frontend/pages/meal-detail/index.vue`
  - 分享、定位、电话、评价按钮补充 toast 反馈。
  - “想去/加入”点击后切换为“已加入”。
- 修改 `frontend/pages/profile/index.vue`
  - 编辑资料、口味标签、发布评价按钮补充 toast 反馈。
  - 个人页内容 Tab 支持切换。

**检查结果：**

- 已用 Node 对 `frontend/preview-static/main.js` 做语法检查，通过。
- 已确认 `http://127.0.0.1:5173` 返回 `200`。
- 当前修改暂不提交、不推送，后续统一同步。

### 登录、编辑资料、发布评价联调

**目标：** 继续完善前端功能，把登录、编辑资料、发布评价从“提示占位”推进到可调后端接口。

**接口封装：**

- 修改 `frontend/services/api.js`
  - API 地址从旧的 `http://localhost:3000/api` 改为 `http://127.0.0.1:3001/api`
  - 新增 token 存取：`authToken`、`currentUser`、`currentProfile`
  - 新增 `authApi.login()` / `authApi.logout()` / `authApi.token()`
  - 修正 `profileApi` 为 `GET /api/profile`、`PUT /api/profile`
  - 新增 `mealApi.review(mealId, data)`，对接 `POST /api/meals/:mealId/reviews`
  - 新增 `restaurantApi` 列表和详情封装

**新增页面：**

- 新增 `frontend/pages/login/index.vue`
  - 支持昵称、学号、学校登录/注册
  - 支持“使用示例账号”
  - 登录成功后保存 token 和用户资料
- 新增 `frontend/pages/edit-profile/index.vue`
  - 读取当前用户资料
  - 支持编辑校区、口味标签、性格/吃饭标签、预算偏好、性别展示
  - 保存时调用 `PUT /api/profile`
- 新增 `frontend/pages/create-review/index.vue`
  - 支持填写饭局 ID、评价对象用户 ID、评分、评价内容
  - 提交时调用 `POST /api/meals/:mealId/reviews`

**页面接入：**

- 修改 `frontend/pages.json`
  - 注册登录、编辑资料、发布评价三个页面路由
- 修改 `frontend/pages/profile/index.vue`
  - “编辑资料”和“口味标签”进入编辑资料页
  - “发布评价”进入发布评价页
  - 个人页显示已登录用户昵称和口味标签
  - 设置按钮进入登录页，方便切换账号
- 修改 `frontend/pages/meal-detail/index.vue`
  - “评价”按钮进入发布评价页

**静态预览接入：**

- 修改 `frontend/preview-static/main.js`
  - 新增登录页、编辑资料页、发布评价页
  - 登录真实调用 `POST /api/auth/login`
  - 保存资料真实调用 `PUT /api/profile`
  - 发布评价真实调用 `POST /api/meals/:mealId/reviews`
  - 预览页使用 `localStorage` 保存登录状态
- 修改 `frontend/preview-static/style.css`
  - 补充表单标签、评分星级、全宽次按钮样式

**联调验证：**

- `POST /api/auth/login` 已通过，示例账号：小林 / 2026001
- `PUT /api/profile` 已通过，可保存中文标签
- `POST /api/meals/1/reviews` 已通过，可发布中文评价
- `GET /api/health` 正常返回 `ok: true`
- `http://127.0.0.1:5173` 正常返回 `200`
- 注意：PowerShell 直接内联中文 JSON 会出现编码乱码，已用 Node + Unicode 转义验证后端中文存储正常。浏览器页面 UTF-8 请求不受该问题影响。
- 当前修改暂不提交、不推送，后续统一同步。

## 2026-07-10

### 本地同步与预览启动

**目标：** 用户表示远程已更新数据，需要拉到本地并启动预览继续看页面。

**已完成：**

- 在本地仓库执行快进拉取，当前已到最新提交：
  - `9d140e3 Connect frontend auth profile and reviews`
- 新增内容包括：
  - `frontend/` 前端目录
  - `data/raw/`、`data/processed/`
  - 多份 UI、接口、数据库、数据清洗文档
  - 根目录 `learn/` 学习文档
- 本地工作区拉取后保持干净。

**启动情况：**

- 后端已启动：
  - 地址：`http://127.0.0.1:3001`
  - 启动目录：`平台代码/backend`
  - 命令：`npm run dev`
- 前端静态预览已启动：
  - 地址：`http://127.0.0.1:5173`
  - 启动目录：`平台代码/frontend`
  - 命令：`npm run preview:static`

**验证结果：**

- `GET /api/health` 正常，返回 `ok: true`、`storage: "sqlite"`。
- 前端静态首页 `http://127.0.0.1:5173` 返回 `200`。

### 登录后首页头像字与“我的”页登录按钮调整

**目标：**

- 用户要求登录后首页左上角从固定“饭”改成“用户名第一个字”。
- 用户要求登录完成后，“我的”页不再显示“登录账号”按钮。

**已修改文件：**

- `frontend/preview-static/main.js`
  - 首页左上角 `.mascot` 从固定 `饭` 改为 `getUserInitial()`。
  - 新增 `getUserInitial()`：
    - 已登录：取 `authSession.user.nickname` 的第一个字符。
    - 未登录：默认显示 `饭`。
  - “我的”页底部登录按钮改为：
    - 未登录：显示 `登录账号`
    - 已登录：不渲染该按钮
- `frontend/pages/home/index.vue`
  - 首页左上角从固定 `饭` 改为 `{{ userInitial }}`。
  - 新增 `currentUser` 状态。
  - 新增 `computed.userInitial`，从 `currentUser.nickname` 取首字。
  - 在 `onShow()` 中读取 `uni.getStorageSync('currentUser')`，保证登录后回首页能刷新。

**检查结果：**

- `node --check 平台代码/frontend/preview-static/main.js` 通过。
- `GET /api/health` 正常。
- `POST /api/auth/login` 正常，示例账号：小林 / 2026001。
- `GET /api/profile` 正常。
- `GET /api/meals?onlyAvailable=true` 正常。
- `GET /api/restaurants` 正常。
- 中文 query 需要 URL 编码；前端 `services/api.js` 已使用 `encodeURIComponent`，浏览器请求正常。

**当前注意：**

- 本轮改动尚未提交、尚未推送。
- 当前本地有未提交改动，主要是：
  - `frontend/preview-static/main.js`
  - `frontend/pages/home/index.vue`
- 后续切 AI 后，建议先执行 `git status --short` 确认。

### 找人页待改

**用户反馈：**

- 用户对当前“找人”页面不太满意。
- 要求暂时保持整体 UI 风格不变。
- 用户准备找类似功能页面参考，再继续重做“找人”页。

**建议下一步：**

- 等用户提供参考图/链接后，先确定“找人页”的核心功能定位：
  - 找饭友
  - 看活跃榜单
  - 按口味/校区/饭局匹配
  - 看共同想去/共同口味
- 主要改动文件预计为：
  - `frontend/pages/find/index.vue`
  - `frontend/preview-static/main.js`
  - 如需样式：`frontend/preview-static/style.css`

### 找人页重做为帖子流模式

**参考界面：** 用户提供了社区/组队 APP 的 UI 结构描述（PostCard 帖子流 + FAB 悬浮按钮）。

**改造内容：**

- **从排行榜 → 帖子流**：原来的”去过/想去/好评”榜单改为找饭友帖子信息流。
- **PostCard 组件结构**（三段式）：
  - Header：头像 + 用户名 + Lv.等级徽章 + 身份标签（大学/实习）+ 发布时间
  - Body：分类标签（约饭/探店/夜宵/拼桌/咖啡，镂空边框，按类别不同颜色）+ 正文内容
  - Footer：”等x人”文字 + 重叠头像组 + 加入按钮（圆形加号）
- **搜索栏**：页面顶部新增圆角搜索栏，占位文案”搜索饭搭子...”
- **分类筛选**：全部 / 约饭 / 探店 / 夜宵 / 拼桌 / 咖啡，点击筛选帖子
- **悬浮发布按钮（FAB）**：右下角黄色圆形加号按钮，sticky 定位，点击跳转发布页
- **加入交互**：点击帖子底部的”+”按钮切换为”已约”（黄色填充），再次点击取消
- **空状态**：筛选无结果时显示 🍽 + “该分类下暂无找饭友帖”

**改动文件：**

- `frontend/pages/find/index.vue` — 完全重写模板、数据和样式
- `frontend/preview-static/main.js` — 新增 findPosts 数据、renderFindPosts()、分类筛选和加入交互
- `frontend/preview-static/style.css` — 新增 ~200 行 PostCard + FAB 样式

**验证结果：**
- ✅ `node --check main.js` 语法检查通过
- ✅ 后端 health 正常（`ok: true, storage: sqlite`）
- ✅ 前端预览 `http://127.0.0.1:5173` 返回 200

**当前注意：**
- 本轮改动尚未提交、尚未推送。
- 本地有未提交改动：
  - `frontend/pages/find/index.vue`（重写）
  - `frontend/preview-static/main.js`
  - `frontend/preview-static/style.css`
  - `frontend/pages/home/index.vue`（之前的首页首字改动）
  - `平台代码/开发记录.md`

### 找人详情页：帖子 → 发起人信息 + 信用分 + 餐厅

**目标：** 找人页帖子卡片点击后进入详情，展示帖子内容、发起人档案（含信用分系统预留）、推荐餐厅信息。

**详情页结构（3个卡片 + 底部操作栏）：**

1. **帖子内容卡片**：头像 + 用户名 + Lv.+ 身份标签 + 时间 + 分类标签 + 正文 + 等人信息
2. **发起人信息卡片**：
   - 大头像 + 口味标签（黄色底）+ 性格标签（紫色底）
   - 个人简介（一行文字描述）
   - **信用分区**：信用分大数字（绿色）+ "信用优秀"徽章
   - 统计行：历史约饭 / 获得评价 / 爽约次数（灰底三栏）
   - 信息行：校区 / 预算偏好
3. **餐厅信息卡片**：大图 + 名称 + 菜系标签 + 评分/位置/人均 + 标签（绿色底）+ 描述文字
4. **底部操作栏**（sticky）：评价按钮 + 约TA按钮（黄色）

**数据增强：**
- findPosts 每篇帖子新增 `author` 字段：campus、creditScore、mealCount、reviewCount、tasteTags、personalityTags、budgetPreference、desc
- findPosts 每篇帖子新增 `restaurant` 字段：name、foodType、location、avgPrice、rating、tags、description、image

**改动文件：**

- `frontend/preview-static/main.js`：
  - findPosts 数据丰富（每个帖子 +author +restaurant）
  - 新增 `find-detail` 页面模板
  - 新增 `renderFindDetail()` 渲染函数
  - 帖子卡片加 `data-find-detail` 属性支持点击跳转
  - 详情页加入/取消、评价按钮交互
- `frontend/preview-static/style.css`：新增 ~300 行详情页样式
- `frontend/pages/find-detail/index.vue`：新建 Vue 版详情页（结构与静态一致）
- `frontend/pages/find/index.vue`：帖子卡片 @tap 跳转详情
- `frontend/pages.json`：注册 `pages/find-detail/index` 路由

**验证结果：**
- ✅ `node --check main.js` 语法检查通过
- ✅ 后端 health 正常
- ✅ 前端预览 `http://127.0.0.1:5173` 返回 200

**当前注意：**
- 本轮改动尚未提交。
- 本地未提交改动新增：`pages/find-detail/index.vue`、`pages.json`

### 本轮提交前整理

**目标：** 用户要求“读一下现在改动的东西，然后写开发记录，推送 GitHub”。

**已读当前改动：**

- `frontend/pages/find/index.vue`
  - 找人页从原排行榜重构为帖子流模式。
  - 支持顶部搜索占位、分类胶囊、帖子卡片、等人数量、头像组、约饭按钮、空状态、悬浮发布按钮。
  - 帖子点击进入找人详情页。
- `frontend/pages/find-detail/index.vue`
  - 新增找人详情页。
  - 展示帖子内容、发起人档案、信用分、历史约饭、获得评价、爽约次数、校区/预算、推荐餐厅和底部操作栏。
- `frontend/pages.json`
  - 注册 `pages/find-detail/index` 路由。
- `frontend/preview-static/main.js`
  - 静态预览同步找人页帖子流。
  - 新增找人详情页渲染、分类筛选、约饭/取消约饭、评价提示、FAB 跳转发布页。
  - 保留登录后首页左上角显示用户名首字。
  - “我的”页已登录时不再显示“登录账号”按钮。
- `frontend/preview-static/style.css`
  - 新增找人页 PostCard、FAB、空状态、找人详情页、信用分卡片、餐厅卡片和底部操作栏样式。
- `frontend/pages/home/index.vue`
  - 登录后首页左上角显示用户名首字，未登录显示“饭”。
- `frontend/pages/profile/index.vue`
  - 移除顶部工具区和装饰饭字。
  - 口味标签改为按钮样式。
  - “新建饭局”“发布评价”改为全宽按钮。
  - 未登录时才显示“登录账号”按钮。

**验证结果：**

- `node --check 平台代码/frontend/preview-static/main.js` 通过。
- `git diff --check` 通过。
- `GET http://127.0.0.1:3001/api/health` 正常，返回 `ok: true`、`storage: "sqlite"`。
- `http://127.0.0.1:5173` 静态预览返回 `200`。

**提交计划：**

- 提交信息建议：`Redesign find page and detail view`
- 推送到 GitHub `main`，方便后续继续与前端合并和预览。

## 当前下一步

建议下一步：

1. **先处理当前未提交改动**：确认找人页全部改动无误后提交/推送。
2. **继续前后端联调**：发布饭局、加入饭局、我的饭局接入真实接口。
3. **补后端接口**：取消饭局、完成饭局、举报/爽约记录。
4. **正式运营前**，将 SQLite 替换为 MySQL（改动范围：新建 `db-mysql.js`，SQL 语句基本复用）。

### 饭局主流程真实接口联调

**目标：** 把发布饭局、首页饭局列表、饭局详情、加入饭局、我的饭局从前端假数据切到后端 SQLite 接口。

**前端页面改动：**

- `frontend/pages/create-meal/index.vue`
  - 发布饭局改为调用 `mealApi.create()`。
  - 登录状态校验：未登录时跳转登录页。
  - 解析预算与时间，提交 `title / foodType / mealTime / place / campus / maxPeople / budgetMin / budgetMax / chatMode / description`。
  - 发布成功后进入对应饭局详情页。
- `frontend/pages/home/index.vue`
  - 首页饭局列表改为调用 `mealApi.list({ onlyAvailable: true })`。
  - 增加加载态、空状态、预算格式化、时间格式化和菜系图片映射。
  - 点击饭局卡片进入真实详情页。
- `frontend/pages/meal-detail/index.vue`
  - 详情页改为调用 `mealApi.detail(id)`。
  - 加入饭局改为调用 `mealApi.join(id)`。
  - 根据参与者列表判断当前用户是否已加入。
  - 未登录点击加入时跳转登录页。
- `frontend/pages/my-meals/index.vue`
  - 我的饭局改为调用 `mealApi.mine()`。
  - 支持“我发起的 / 我加入的”两类数据。
  - 增加加载态、空状态和详情跳转。
- `frontend/services/api.js`
  - 新增 `mealApi.mine()`，对应 `GET /api/meals/mine`。

**静态预览改动：**

- `frontend/preview-static/main.js`
  - 首页今日饭局接入 `GET /api/meals?onlyAvailable=true`。
  - 饭局详情接入 `GET /api/meals/:id`。
  - 发布饭局接入 `POST /api/meals`。
  - 加入饭局接入 `POST /api/meals/:id/join`。
  - 新增“我的饭局”预览页，接入 `GET /api/meals/mine`。
  - 评价页默认饭局 ID 跟随当前打开的饭局。
  - 增加预算、时间、菜系图片等数据映射函数。

**验证结果：**

- `node --check 平台代码/frontend/preview-static/main.js` 通过。
- `git diff --check` 通过。
- `GET http://127.0.0.1:3001/api/health` 返回 200。
- `GET http://127.0.0.1:5173` 返回 200。

### 队友前端协作文档

**目标：** 用户要求把 GitHub 链接、SSH 地址、队友拉取与本地运行说明整理成 Word 文档，并推送到 GitHub。

**新增文件：**

- `docs/队友前端协作说明.docx`

**文档内容：**

- GitHub 页面链接与 SSH 克隆地址。
- SSH key 权限说明与测试命令。
- 前端主要目录与重点文档。
- 前端静态预览、本地后端运行方式。
- 建议的分支协作流程。
- 当前已连后端的流程清单。
- 当前仍偏演示态的模块清单。
- 可直接发给队友的短版消息。

**当前注意：**

- 本轮准备提交并推送到 GitHub `main`。
- 本地接口串联测试通过：登录 -> 发布饭局 -> 读取详情 -> 读取我的饭局。
- 已清理联调时生成的 smoke-test 测试饭局，避免污染预览列表。

**当前注意：**

- 本轮改动尚未提交、尚未推送。
- 下一步建议继续补：取消加入、完成饭局、饭局状态标签、首页筛选条件真正传给后端。

### 按钮与后端联通性扫描

**目标：** 扫一遍当前前端按钮，确认哪些已连后端，哪些仍是本地演示/占位。

**本轮修正：**

- `frontend/pages/profile/index.vue`
  - “我的饭局”区域新增真实入口，点击进入 `pages/my-meals/index`。
  - 未登录点击“我的饭局”会先跳登录。
  - “新建饭局”保留为跳转发布饭局。
- `frontend/pages/find/index.vue`
  - 右下角 FAB 从“发帖功能即将上线”改为跳转 `pages/create-meal/index`，先复用发布饭局能力。

**已连后端的按钮/流程：**

- 登录 / 示例登录：`POST /api/auth/login`
- 编辑资料 / 保存资料：`GET /api/profile`、`PUT /api/profile`
- 发布饭局：`POST /api/meals`
- 首页饭局列表：`GET /api/meals`
- 饭局详情：`GET /api/meals/:id`
- 加入饭局：`POST /api/meals/:id/join`
- 我的饭局：`GET /api/meals/mine`
- 发布评价：`POST /api/meals/:id/reviews`

**仍是本地演示或占位的按钮/模块：**

- 首页“想去”爱心：当前只做本地切换，后端暂无 wishlist/favorite 表。
- 首页筛选胶囊（全校/品类/时间/筛选）：当前只切 UI 状态，尚未把筛选条件传给 `GET /api/meals`。
- 饭局详情“地图导航”“联系发起人”“分享”：当前是 toast，占位；后端已有地点字段，但未接地图/联系方式。
- 饭局详情“取消加入”：后端已有 `POST /api/meals/:id/leave`，前端按钮还没做成“已加入后可取消”。
- 动态页：整页是静态 feed 数据，关注/推荐、分类、想去都未接后端。
- 找人页/找人详情：帖子数据是静态数组，约 TA 是本地切换；后端暂无 posts/match_request 表。
- 我的页统计数字（关注/饭搭子/好评与想去）：当前是固定展示，未从后端统计。
- 我的页“去过/想去/动态/收藏”：当前只切 tab 样式，未接真实数据。

**验证结果：**

- `node --check 平台代码/frontend/preview-static/main.js` 通过。
- `git diff --check` 通过。
- `GET http://127.0.0.1:3001/api/health` 返回 200。
- `GET http://127.0.0.1:5173` 返回 200。

## 2026-07-14

### 补充饭局取消、完成与举报/爽约记录功能

**变更：**
- `backend/src/db.js`：
  - 新增 `reports` 表定义（保存举报关系及状态）。
  - 新增 `normalizeReport` 结构转换函数。
  - 新增并导出数据库操作函数：`getReport`、`createReport`、`listReports`。
- `backend/src/server.js`：
  - 新增 API 路由及处理函数：
    - `POST /api/meals/:mealId/cancel`：取消饭局（仅发起人可用，必须是 `open` 或 `matched` 状态）。
    - `POST /api/meals/:mealId/finish`：标记饭局完成（仅发起人可用，必须是 `matched` 状态）。
    - `POST /api/meals/:mealId/reports`：举报/爽约（限同局参与者，不能自举报，且目标用户须存在于该饭局）。

**验证结果：**
- ✅ `npm run check` 语法检查通过。
- ✅ 经 `curl` 请求模拟覆盖验证，状态转换与非法越权操作校验均正确拦截。
- ✅ 数据库记录成功持久化至 SQLite。

## 2026-07-15

### 联通退出饭局、首页筛选与找人帖子流

**变更：**
- **1. 退出饭局 (Leave Meal)**
  - **后端**：已支持 `POST /api/meals/:mealId/leave` 逻辑，处理成员退局，退局后若原状态为 `matched`（满员）则动态重置为 `open` 并扣减人数。
  - **前端**：在 [meal-detail/index.vue](file:///Users/shenglin/Desktop/杂乱文件/饭饭之交文件/平台代码/frontend/pages/meal-detail/index.vue) 与静态预览中打通退出接口。在详情页加入了精细的身份状态判断：发起人显示“我是发起人”且禁用；已加入成员显示“退出饭局”，点击调用退出接口；未加入成员显示“想去/加入”，点击调用加入接口。
- **2. 首页筛选 (Home Filters)**
  - **后端**：`GET /api/meals` 增加了对 `campus` 和 `keyword`（品类关键词）的解析与数据库过滤功能。
  - **前端**：在 [home/index.vue](file:///Users/shenglin/Desktop/杂乱文件/饭饭之交文件/平台代码/frontend/pages/home/index.vue) 与静态预览中打通筛选查询。将顶部胶囊改为循环点击切换（例如：全校 -> 主校区 -> 西校区，全部品类 -> 麻辣香锅 -> 米线...），每次点击均自动重查后端刷新页面。
- **3. 找人帖子流联通后端 (Find Posts API & DB)**
  - **数据库**：新建了 `posts`（找人贴）与 `post_participants`（帖子加入关系）两张 SQLite 数据表，并在种子数据中预置了 5 条精致的演示帖子。
  - **后端**：在 [server.js](file:///Users/shenglin/Desktop/杂乱文件/饭饭之交文件/平台代码/backend/src/server.js) 中新增了增删查改和响应接口：
    - `GET /api/posts`（支持 category 分类筛选）
    - `POST /api/posts`（发布找人帖）
    - `POST /api/posts/:id/join` / `POST /api/posts/:id/leave`（约 TA 与取消）
  - **前端**：在 [find/index.vue](file:///Users/shenglin/Desktop/杂乱文件/饭饭之交文件/平台代码/frontend/pages/find/index.vue)、[find-detail/index.vue](file:///Users/shenglin/Desktop/杂乱文件/饭饭之交文件/平台代码/frontend/pages/find-detail/index.vue) 和静态预览中完整打通了找人贴列表加载、卡片头像组展示、加入/退出帖子约饭状态的同步。

**验证结果：**
- ✅ 前后端服务（`3001` 与 `5173` 端口）正常运行，验证通过。
- ✅ 首页筛选、详情退局、找人流响应功能等均通过真实接口读写 SQLite 且数据一致。

## 2026-07-16

### 新增高级筛选、个人资料扩充与我的饭局看板

**变更：**
- **1. 高级筛选弹窗 (Advanced Filters)**
  - **后端**：在 [db.js](file:///Users/shenglin/Desktop/杂乱文件/饭饭之交文件/平台代码/backend/src/db.js) 与 [server.js](file:///Users/shenglin/Desktop/杂乱文件/饭饭之交文件/平台代码/backend/src/server.js) 中扩充了 `listMeals` 接口，支持对 `minBudget`、`maxBudget`、`minPeople`、`maxPeople` 的参数解析及 SQL 逻辑生成，并支持按价格和人数排序（`sortBy`）。
  - **前端**：在静态页首页新增了“高级筛选弹窗”，支持对预算区间、就餐校区、人数限制、排序方式的高级筛选和重置，交互与真实后台路由完全连通。
- **2. 学生资料扩充与四核心模块建表 (Profile Extensions & Visited/Wishlist/Moments/Saved tables)**
  - **资料表扩充**：支持专业 (`major`)、年级 (`grade`)、星座 (`constellation`)、家乡 (`hometown`) 和简介描述 (`description`)，通过 pragma 动态判定列存在与否进行数据库 Alter Table 迁移。
  - **去过/想去/动态/收藏**：新建了 `visited_restaurants`、`wishlist_restaurants`、`user_moments`、`saved_meals` 数据表，预置种子数据逻辑，并在后端定义了相关的 CRUD 操作及 API 响应 handler。
- **3. 我的饭局数据概览看板 (My Meals Dashboard)**
  - **前端**：在静态预览的“我的饭局”页顶部新增了一个精致的可视化 Dashboard，包含发起/参与次数实时统计、历史拼伙开销总预算动态累加求和，以及美观的约饭打卡热力点网格，视觉效果极佳。

**验证结果：**
- ✅ 前后端服务（`3001` 与 `5173` 端口）重新测试正常，数据库升级脚本无故障。
- ✅ 高级筛选、资料修改、数据看板均能通过真实接口读写 SQLite 且数据一致。

## 2026-07-17

### 打通首页红心想去收藏与想去Tab的动态数据渲染

**变更：**
- **1. 首页红心收藏 (Want/Wishlist Toggle)**
  - **后端**：在 [db.js](file:///Users/shenglin/Desktop/杂乱文件/饭饭之交文件/平台代码/backend/src/db.js) 新增并导出了 `findRestaurantByName` 与 `toggleWishlistRestaurant` 辅助函数。重写了 `handleAddWishlist` 为 `toggle` 操作。
  - **前端**：在 `renderMealCard` 中，接受 `isWished` 参数，动态渲染红心的 `active` 状态（红色实心 `♥` 或空心 `♡`）；在 `bindPageActions` 中，点击红心后发送 `POST /api/profile/wishlist` 切换状态，即时弹出 Toast 提示。
- **2. 想去 Tab 动态数据加载与卡片设计 (Dynamic Wishlist Rendering)**
  - **前端**：在 `profile` 面板中，为去过/想去/动态/收藏配置了 `data-profile-tab` 绑定。实现 `loadProfilePage` 和 `loadProfileTabContent` 异步数据拉取。切换至想去时，调用 `/api/profile/wishlist` 获取真实收藏餐厅，美化了卡片样式（类别、星级、消费、添加时间等）。
  - **视觉优化**：移除了卡片左侧多余的心型与手势图标，在卡片右侧利用 Flex 布局新增了对应的餐厅缩略图（item-thumb），提升了整体的美观度和用户体验。
- **3. 我的饭局预览列表真实化 (My Meals Preview Realization)**
  - **前端**：删除了原本在“我的”页面上方静态的“餐盘图片占位卡片”，替换为动态的 `#profile-my-meals-preview` 容器。
  - **动态获取**：在页面加载时调用 `loadProfileMyMealsPreview()` 请求 `/api/meals/mine` 接口，将用户最新发起和加入的 3 场活跃饭局以带状态标签（“招募中”/“已成局”）、就餐地点和精简排版的条目形式实时列出，点击条目可直接跳转查看详情，点击下方“全部饭局”按钮亦可直达看板页。

**验证结果：**
- ✅ 通过 API 测试脚本 [test_wishlist.js](file:///Users/shenglin/.gemini/antigravity-ide/brain/0042a8cd-f62c-43ad-b95f-77982af19ca2/scratch/test_wishlist.js) 验证了想去添加、去重、删除及字段完整性，100% 成功。
- ✅ 前端个人中心“我的饭局”预览部分已成功接入真数据流。
- ✅ 前后端 JS 语法校验通过，页面流转无断层。

### 首页大众点评式校园美食推荐与店铺详情重构

**变更：**
- **1. 首页重构 (Yelp/Dianping Homepage Pivot)**
  - **后端**：在 [db.js](file:///Users/shenglin/Desktop/杂乱文件/饭饭之交文件/平台代码/backend/src/db.js) 的 `listRestaurants` 中支持了价格区间 (`minPrice` / `maxPrice`) 过滤及按评分 (`rating-desc`) / 价格 (`price-asc`) 排序。
  - **前端**：将首页“今日饭局”版块替换为“今天你想吃什么”美食店铺推荐流；设计了精美的 `.restaurant-card` 点评样式（包含缩略图、评分星级、人均消费、校区地址和特色标签胶囊）；第一个筛选胶囊保留全校，第三个由“时间”改为“排序”（智能排序、评价最高、价格最低）。
- **2. 店铺详情与一键发起 (Restaurant Detail & Auto-Prefill)**
  - **前端**：新增了 `restaurant-detail` 店铺详情页模板；展现店铺大图、全称、评分、简介及标签；展示“在此店约饭的同学”活动项；提供“在此店发起约饭”跳转，路由至发布表单时**自动预填**地点输入框和饭局标题。
- **3. 数据库数据迁移 (Database Migrations)**
  - **迁移**：为 `meals` 表新增了 `restaurant_id` 关联字段，并编写了向下兼容 SQL，在服务启动时自动对已有的历史饭局记录按就餐地点的文本相似度匹配归档到对应的餐厅 ID，实现无缝整合。

**验证结果：**
- ✅ 通过 API 测试脚本 [test_restaurant_pivot.js](file:///Users/shenglin/.gemini/antigravity-ide/brain/0042a8cd-f62c-43ad-b95f-77982af19ca2/scratch/test_restaurant_pivot.js) 验证了餐厅列表、条件过滤、外键关联约饭检索等，100% 成功。
- ✅ 前后端编译与 JS 语法校验通过，页面导航和表单自动预填表现灵敏。

## 当前下一步

建议下一步：
1. **正式运营前**，将 SQLite 替换为 MySQL。
2. **继续打通其它 Tab 面板列表的数据联动**。

## 2026-07-18

### 接入独立餐厅数据库 restaurants.db

**目标：** 用户确认餐厅数据已经迁移到根目录 `database/data/restaurants.db`，后端餐厅接口应优先读取该库，不再依赖 `backend/data/fanfan.db` 里的旧随机/种子餐厅。

**本轮处理：**

- `backend/src/db.js`
  - 确认并保留 `RESTAURANT_WAREHOUSE_PATH`：`database/data/restaurants.db`。
  - `listRestaurantCatalog()` 继续从独立餐厅库读取真实清洗数据，返回 `source: "restaurant_warehouse"`。
  - 新增/完善餐厅 ID 兼容逻辑：新库餐厅 ID 为字符串，旧业务库餐厅 ID 为数字。
  - `findRestaurantByName()` 优先从独立餐厅库按关键词查找，再回退旧餐厅表。
  - `addVisitedRestaurant()`、`addWishlistRestaurant()`、`toggleWishlistRestaurant()` 改为：
    - 数字 ID：继续写旧 `restaurant_id`。
    - 字符串 ID：不强塞进旧整数外键，改为保存 `restaurant_name`。
  - `createMeal()`、`createPost()` 避免把新库字符串 ID 写入旧整数外键。
  - 修复全新 `fanfan.db` 启动时 `seedNewTablesIfEmpty()` 早于用户种子写入导致的外键失败。
  - 将默认“去过/想去”种子数据从旧餐厅外键改为餐厅名称存储，降低对旧随机餐厅表的依赖。
- `backend/src/server.js`
  - `GET /api/meals?restaurantId=` 不再把字符串餐厅 ID 强制转为 `Number()`。
  - `POST /api/posts` 不再把字符串餐厅 ID 强制转为 `Number()`。
  - `GET /api/restaurants/:id` 支持读取 `campus` query，用于新库候选门店按校区距离选择。

**验证结果：**

- `node --check backend/src/db.js` 通过。
- `node --check backend/src/server.js` 通过。
- `git diff --check` 通过。
- 本地后端启动成功。
- `GET /api/restaurants?page=1&pageSize=3&campus=cufe_shahe` 返回：
  - `source: "restaurant_warehouse"`
  - `total: 136`
  - `items: 3`
- `GET /api/restaurants/:id?campus=cufe_nanlu` 可用新库字符串 ID 读取详情。
- `GET /api/meals?restaurantId=<新库字符串ID>` 不再报错，当前返回空列表属于正常兼容结果。

**当前注意：**

- `backend/data/fanfan.db` 是业务库，仍用于用户、饭局、评价、动态等业务数据。
- `database/data/restaurants.db` 是餐厅清洗库，餐厅列表/详情优先读取它。
- 本地启动后端会让 `backend/data/fanfan.db` 自动 seed/checkpoint，因此工作区可能显示该文件被修改；提交时需要确认是否真的要提交业务库变化。

### 餐厅列表分页批量修正

**目标：** 用户指出餐厅列表应当“往下滑动后 6 张、6 张加载”，检查当前实现是否符合。

**检查结果：**

- 首页推荐区：`pageSize=6`，符合预期。
- 静态预览：`pageSize=6`，符合预期。
- 小程序餐厅列表页 `frontend/pages/restaurant-list/index.vue`：原先 `PAGE_SIZE = 12`，不符合预期。

**本轮修正：**

- `frontend/pages/restaurant-list/index.vue`
  - 将 `PAGE_SIZE` 从 `12` 改为 `6`。
  - 保留 `onReachBottom()` 触发 `loadNextPage()` 的分页加载逻辑。
  - 每次请求仍带 `page / pageSize / campus`，后端按页切片返回。

**验证结果：**

- `GET /api/restaurants?page=1&pageSize=6&campus=cufe_shahe` 返回 6 条，`total=136`，`hasMore=true`。
- `GET /api/restaurants?page=2&pageSize=6&campus=cufe_shahe` 返回 6 条，且与第一页 ID 不重复。
- `node --check backend/src/db.js` 通过。
- `node --check backend/src/server.js` 通过。
- `git diff --check` 通过。

### 首页餐厅推荐流下滑 6 家追加加载

**目标：** 用户澄清“6 张 6 张加载”主要指首页推荐流：进入首页先显示 6 家餐厅，页面滑到底后再追加下一页 6 家，而不是只在餐厅列表页生效。

**本轮修正：**

- `frontend/pages/home/index.vue`
  - 首页新增餐厅推荐分页状态：`restaurantPage`、`restaurantHasMore`、`restaurantLoadingMore`。
  - 首页 `onReachBottom()` 调用追加加载逻辑，每次请求 `pageSize=6`。
  - 切换校区、品类、排序时重置首页餐厅分页，从第一页重新加载。
  - 首屏加载、继续加载、到底提示分离，避免追加分页时把已有卡片清空。
- `frontend/preview-static/main.js`
  - 静态预览同步新增首页分页状态：`homeRestaurantPage`、`homeRestaurantHasMore`、`homeRestaurantLoading`。
  - 给预览容器增加滚动监听，滚动到底后请求下一页并 append 到 `#home-restaurant-list`。
  - 筛选 pill 和高级筛选确认后会重置首页分页。
  - 首页餐厅卡片保留新餐厅库字符串 ID（如 `M018`），点击详情不再强制转成数字。
- `frontend/pages/restaurant-list/index.vue`
  - 保持每页 `PAGE_SIZE = 6`，列表页也继续符合 6 条分页加载。

**验证结果：**

- `node --check frontend/preview-static/main.js` 通过。
- `node --check backend/src/db.js` 通过。
- `node --check backend/src/server.js` 通过。
- `git diff --check` 通过，仅出现 Git 换行风格提示。
- `GET /api/restaurants?page=1&pageSize=6&campus=cufe_shahe` 返回 6 条。
- `GET /api/restaurants?page=2&pageSize=6&campus=cufe_shahe` 返回 6 条，`total=136`，`hasMore=true`。
- 本地预览 `http://127.0.0.1:5173` 返回 200。

### 首页标签搜索与餐厅库分类检查

**目标：** 首页搜索框支持直接输入标签或关键词搜索餐厅，例如“火锅”“清真”“近沙河”“面”“烧烤”“适合聚餐”，搜索结果继续保持 6 家一页分页追加。

**本轮修正：**

- `backend/src/db.js`
  - 新增餐厅搜索词拆分与近场词兼容：例如“近沙河”会同时尝试匹配“近沙河”和“沙河”。
  - 为餐厅生成内部搜索文本：`name + cuisine + recommended_dishes + optional_dishes + full_address + location_hint + party_size + booking_note + sentiment + cleaning_note + raw_messages + amap_category + POI候选名称/地址/分类 + 派生tags`。
  - 返回餐厅时新增 `tags`，来源包括菜系、推荐菜、可选菜、高德分类、位置提示、聚餐/清真/群友推荐等派生标签。
  - keyword 过滤改为合并餐厅与候选门店、派生 tags 之后再过滤，保证“适合聚餐”这类非原始字段标签也能搜到。
- `frontend/pages/home/index.vue`
  - 首页搜索框改为可输入，支持确认搜索、右侧搜索按钮搜索、输入防抖搜索。
  - 搜索请求携带 `keyword`，并与原有 `page/pageSize/campus/foodType/sortBy` 一起工作。
  - 搜索或清空搜索时重置首页分页，继续按 6 家一页加载。
- `frontend/preview-static/main.js` 与 `frontend/preview-static/style.css`
  - 静态预览首页同步支持关键词输入、回车搜索、搜索按钮和防抖搜索。

**数据库检查：**

- `database/data/restaurants.db` 当前餐厅总数：136。
- `cuisine` 为空的餐厅：33 家。
- `cuisine` 含“推测/需确认/依据上下文”的半分类餐厅：22 家。
- `recommended_dishes` 为空的餐厅：77 家。
- 重点未分类样例：`M007 匠熙小馆`、`M016 1733`、`M020 徐小黑`、`M028 腊罗巴`、`M038 嫩牛家`、`M039 沙胆彪`、`M068 蚝叔`、`M073 锦炉`、`M094 蜗牛小馆`、`M101 小木船`。
- 后续建议：优先补 `cuisine` 为空且 `mention_count > 0` 的老数据，再补新增的 `M115-M136` 批次。

**验证结果：**

- `node --check backend/src/db.js` 通过。
- `node --check backend/src/server.js` 通过。
- `node --check frontend/preview-static/main.js` 通过。
- `git diff --check` 通过，仅出现 Git 换行风格提示。
- 已重启本地后端 `http://127.0.0.1:3001`。
- `GET /api/restaurants?keyword=火锅&page=1&pageSize=6&campus=cufe_shahe` 返回 `total=26`，本页 6 条。
- `GET /api/restaurants?keyword=清真&page=1&pageSize=6&campus=cufe_shahe` 返回 `total=5`。
- `GET /api/restaurants?keyword=适合聚餐&page=1&pageSize=6&campus=cufe_shahe` 返回 `total=7`，本页 6 条。

### 餐厅信息半自动补全脚本

**目标：** 用户询问是否可以写脚本半自动补充餐厅分类、标签、推荐菜等信息。当前方案采用“规则自动建议 + 人工审核 + 可选高置信应用”，避免直接把不可靠推断写进正式库。

**本轮新增：**

- `database/src/suggest-classification.mjs`
  - 扫描 `database/data/restaurants.db` 的餐厅、POI 候选、高德分类、群聊原文、推荐菜和位置提示。
  - 根据店名、菜系、高德分类、原始文本规则推断 `cuisine`、推荐菜和标签。
  - 生成 `reviewStatus=high_confidence / needs_review` 两类建议。
  - 默认只输出审核文件，不修改数据库。
  - 支持 `--apply-high-confidence`，只应用高置信建议，并写入 `sync_log`。
- `database/data/classification-suggestions.json`
  - 机器可读审核队列，适合后续做审核页面或二次处理。
- `database/data/classification-suggestions.csv`
  - Excel 可读审核表，方便人工确认。
- `database/package.json`
  - 新增 `classify:suggest` 和 `classify:apply-high-confidence` 脚本。
- `database/src/lib.mjs`
  - 修复数据库路径：改为基于 `database/src/lib.mjs` 所在位置解析，避免从 `database` 子目录运行脚本时误建 `database/database/data/restaurants.db`。
- `database/README.md`
  - 增加半自动分类补全用法说明。

**验证结果：**

- 运行 `node database/src/suggest-classification.mjs` 成功。
- 输出建议总数：136。
- 高置信建议：48。
- 需要人工审核：88。
- 已验证 JSON 可被 Node 正常解析。
- 已生成 CSV：`database/data/classification-suggestions.csv`。
- `node --check database/src/suggest-classification.mjs` 通过。
- `node --check database/src/lib.mjs` 通过。
- 未执行 `--apply-high-confidence`，因此正式 `restaurants.db` 没有被本脚本批量改写。

**注意：**

- 自动分类只能作为“第一轮草稿”，高德分类和店名关键词会误判，尤其是同名店、商场店、综合餐饮分类。
- 建议优先人工审核高置信的 48 条，再决定是否开放自动应用。
## 2026-07-18

### 首页精选专辑横滑区样式整理

**目标：** 用户指出首页“精选专辑”区域第三张卡片被裁切明显，底部还能看到横向滚动条，视觉上不像参考 App 的自然横滑专辑区。

**本轮修复：**

- `frontend/preview-static/style.css`
  - 专辑卡改为一屏完整露出 3 张。
  - 图片统一为方形圆角卡片。
  - 隐藏浏览器横向滚动条，保留横向滑动能力。
  - 标题、副标题增加单行省略，避免长文案撑破卡片。
  - 增加轻量 scroll snap，让横滑停靠更自然。
- `frontend/pages/home/index.vue`
  - uni-app 首页专辑卡同步收窄为三张一屏。
  - 图片改为方形比例，标题和副标题增加省略保护。
  - 调整专辑图上的城市大字尺寸，避免压住下方文案。

**验证结果：**

- `node --check frontend/preview-static/main.js` 通过。
- `git diff --check` 通过，仅有 Git 换行风格提示。

## 2026-07-18

### 餐厅详情顶部图片沿用首页照片

**目标：** 用户指出从首页餐厅卡片点进详情后，顶部大图应该继续使用首页展示的同一张餐厅照片，而不是按菜系重新猜一张图。

**本轮修复：**

- `frontend/preview-static/main.js`
  - 新增 `getRestaurantImage()`，统一餐厅图片取值顺序：`photoUrl -> image -> coverUrl -> imageUrl -> 菜系兜底图`。
  - 首页餐厅卡片改为使用 `getRestaurantImage()`。
  - 餐厅详情页顶部 `hero` 大图改为使用 `getRestaurantImage(restaurant)`。
  - 通用餐厅卡片 `renderRestaurantCard()` 也改为同一套图片逻辑，避免不同入口图片不一致。
  - 图片加载失败时继续回退到默认餐厅图，避免坏图破坏页面。

**验证结果：**

- `node --check frontend/preview-static/main.js` 通过。

## 2026-07-18

### 修复餐厅详情空数值 toFixed 报错

**问题：** 本地预览打开新餐厅库详情时出现 `加载失败: Cannot read properties of undefined (reading 'toFixed')`。原因是新接入的 `database/data/restaurants.db` 里部分餐厅没有旧字段 `rating` / `avgPrice`，前端仍按旧随机种子数据直接调用 `.toFixed()`。

**本轮修复：**

- `frontend/preview-static/main.js`
  - 新增 `formatRestaurantRating()`、`formatRestaurantPrice()`、`getRestaurantCuisine()`、`getRestaurantLocationText()`。
  - 餐厅卡片、详情页、我的页面去过/想去列表不再直接调用 `restaurant.rating.toFixed()` 或 `(restaurant.avgPrice / 100).toFixed()`。
  - 缺评分时显示 `暂无评分`，缺人均价格时显示 `人均待补`。
- `frontend/pages/find-detail/index.vue`
  - 推荐官动态里的餐厅人均价改为安全格式化方法。
  - 兼容 `avgPrice`、`averagePrice` 为空或单位不同的情况。

**验证结果：**

- `GET /api/restaurants/M018?campus=cufe_shahe` 确认新库详情存在 `rating: null`、`avgPrice: null`，复现原因明确。
- `node --check frontend/preview-static/main.js` 通过。
- `git diff --check` 通过，仅有 Git 换行风格提示。

## 2026-07-20

### 极简登录窗口设计与一键 Demo 账号接入

**变更：**
- `frontend/preview-static/main.js`
  - 重构 `pages.login` 模板为精美卡片样式，引入可爱 Icon 头部、清晰字段输入框。
  - 新增一键快捷登录胶囊按钮（`data-demo-user`），支持点选 `小林 (主校区 · 计算机系)`、`阿晴 (主校区 · 设计系)`、`米雪 (西校区 · 软件工程)` 快捷充填并即时调用 `/api/auth/login` 登录。
- `frontend/preview-static/style.css`
  - 新增 `.login-page-card`、`.login-avatar-wrap`、`.demo-accounts-section`、`.demo-pill` 样式，高质感卡片布局。

**验证结果：**
- `node --check frontend/preview-static/main.js` 编译检查通过。

## 2026-07-21

### 教务处课表对接、DOM 自动拉取与无课空闲分析

**变更：**
- **1. 数据库支持 (`db.js`)**
  - 新增 `user_timetables` 表，保存 `courses_json` 和 `free_slots_json`。
  - 新增 `analyzeFreeTimeSlots` 算法，自动识别全天无课、下午无课与晚间无课时段。
  - 新增 `saveUserTimetable` 与 `getUserTimetable` 存储方法。
- **2. 后端 API 接口 (`server.js`)**
  - 新增 `POST /api/timetable/import`：接收 Webview/前端导出的课表数据并进行智能无课分析入库。
  - 新增 `GET /api/timetable/mine`：读取用户当前课表及无课分析报告。
- **3. 前端与测试视图 (`preview-static/main.js` & `style.css`)**
  - 新增 `timetableImport` 测试与导出页面，支持教务处 URL 导航、Webview DOM 解析模拟器。
  - 内置 **“🧪 载入示范课表数据”** 离线一键测试开关。
  - 自动渲染课程周网格、无课空闲标签与可一键复制的标准导出 JSON 代码块。

**验证结果：**
- ✅ 通过 API 测试脚本 [test_timetable_import.js](file:///Users/shenglin/.gemini/antigravity-ide/brain/0042a8cd-f62c-43ad-b95f-77982af19ca2/scratch/test_timetable_import.js) 验证了课表入库与“周五全天无课/周一下午无课”等智能分析的 100% 准确性。
- ✅ 前后端编译检查与 JavaScript 语法校验通过。

## 2026-07-22

### 课表同步对接与环境依赖修复

**变更：**
- **1. 修复后端包引用与依赖崩溃**
  - 在 `server.js` 中补全引入 `crypto` 与 `cheerio` 依赖模块，解决调用登录和解析时 `cheerio is not defined` 崩溃问题。
  - 针对 `better-sqlite3` 与 Node.js 运行时版本不匹配（v24 编译模块无法在 v22 运行）导致的底层 binary 加载失败 Bug，执行了 `npm rebuild` 并正常跑通启动。
- **2. 修复加密逻辑与 Session 凭证更新 Bug**
  - 修复 RSA 加密中 Exponent 参数 `'10001'`（奇数长度，5 位）在 Node.js `Buffer.from(..., 'hex')` 转换时由于未补零导致指数计算错乱变成 `'EAA'` 的问题，增加了自动前缀补零以确保结果始终为标准的 `'AQAB'`。
  - 在 `handleProxyLogin` 流程中增加了登录成功后抓取并更新最新 `JSESSIONID` 凭证的机制，防止因为 session 更新而导致拉取课表页面时因旧会话 Cookie 失效被教务处拦截。
- **3. 前端课表展示网格与入口集成**
  - 在 `frontend/services/api.js` 中增加了课表模块的 API 封装（含导入、详情、验证码、代理登录）。
  - 注册页面路由 `pages/timetable-import/index` 到 `pages.json`。
  - 在个人中心页面 `pages/profile/index.vue` 引入“教务处课表同步”胶囊卡片组件，点击即可跳转至同步页面，并根据后端 mine 数据动态展示当前课表同步状态。
  - 新建 `pages/timetable-import/index.vue` 页，包含完整的验证码加载刷新机制、一键代理同步表单、响应式 Flexbox 课表网格以及无课空闲时段的智能分析卡片渲染。

**验证结果：**
- ✅ 后端原生 `http` 校验及语法测试 `npm run check` 100% 通过。
- ✅ 验证码接口正常渲染，后台服务重构后稳定运行于 `http://127.0.0.1:3001`。
